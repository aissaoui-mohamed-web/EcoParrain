// MODE HORS-LIGNE / DÉMO
// Firebase a été désactivé à la demande de l'utilisateur pour permettre une utilisation immédiate sans configuration serveur.

export const isConfigured = true;

// Objets factices pour ne pas casser les imports existants, mais ils ne seront plus utilisés.
export const auth = {} as any;
export const db = {} as any;
