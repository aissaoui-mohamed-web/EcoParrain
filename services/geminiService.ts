// Service IA désactivé pour simplifier l'application.
// Ce fichier est conservé comme placeholder pour éviter les erreurs d'import.

export const sendMessageToCoach = async (
  message: string, 
  history: any[]
): Promise<string> => {
  return "Le service de coaching IA est actuellement désactivé.";
};